VITE_API_URL=http://localhost:3000
# Origen de datos por defecto para la pantalla (respaldo de visualización).
# El origen real lo controla el bridge con DATA_SOURCE. Valores: modbus | db
VITE_DATA_SOURCE=modbus
